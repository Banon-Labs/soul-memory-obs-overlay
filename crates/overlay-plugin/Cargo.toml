[package]
name = "overlay-plugin"
version = "0.1.3"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
overlay-proto = { path = "../overlay-proto" }

[target.'cfg(windows)'.dependencies]
interprocess = "2"
obs-wrapper = "0.4"
